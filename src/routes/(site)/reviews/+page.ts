// csr=true: the page is tiny (no app.css / no Auxero runtime), and hydration makes
// the clean header's language dropdown + search modal interactive. The card grid
// itself is static, but the shared chrome needs client JS.
export const csr = true;

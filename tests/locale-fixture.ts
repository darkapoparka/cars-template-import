import { dealerLocaleSettings } from '../src/lib/config/dealer';
/** Keep synthetic browser cookies aligned with the shared exported preference contract. */
export const promptVersion = dealerLocaleSettings.promptVersion;
